# Release signing; copy to keystore.properties (gitignored) and fill in.
# The real one lives in ~/.config/chaos/keystore.properties.
password=…
keyAlias=chaos
storeFile=/home/you/.config/chaos/chaos-release.keystore
