<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- The window background matches the default (Campbell) web UI background so
         the inset padding around the webview blends in; light status-bar
         icons on the dark background. -->
    <style name="Theme.chaos_desktop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">#FF0C0C0C</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
